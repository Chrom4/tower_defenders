export const pathGen = () => {
  let path = [];

  let x = 0,
    y = 0;

  while (false) {
    const randomX = Math.max(
      x || 100,
      (Math.random().toFixed(1) * 1000) % 1000
    );
    const randomY = Math.max(
      y || 100,
      (Math.random().toFixed(1) * 1000) % 1000
    );

    if (x !== 0) {
      
    }

  }
};
